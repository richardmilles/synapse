/**
 * Affichage des sections de la page d'accueil.
 * 1 = la section est affichée, 0 = la section est masquée (le code reste en place).
 */
export const homeSections = {
  /** Bandeau "Ils nous font confiance" (logos clients) */
  clientLogos: 0,
  /** Barre de chiffres clés (5 expertises / 4 étapes / 48h / 100%) */
  proofBar: 0,
};
